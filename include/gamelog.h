#ifndef GAMELOG_H
#define GAMELOG_H

#include <string>
#include <vector>

using namespace std;

class GameLog {
 public:
  GameLog();
  ~GameLog();
  // Turn *pop(int n);
  // void push(Turn *);
};

#endif